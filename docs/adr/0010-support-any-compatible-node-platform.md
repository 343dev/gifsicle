# Support any compatible Node.js platform

The package will not maintain an operating-system or CPU allowlist: any environment providing the supported Node.js version, Worker Threads, and WebAssembly may run the same distribution. CI guarantees behavior on Linux, macOS, and Windows for x64 and ARM64, while other environments are permitted rather than rejected solely because they are absent from that matrix.
