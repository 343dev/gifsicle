# Isolate exact parity exceptions

Native/WebAssembly byte mismatches are release blockers unless a specific fixture-and-options case is investigated and recorded with its input hash, both output hashes and sizes, and independently verified playback equivalence. The project will not exempt whole option families such as numeric gamma or use a generic visual tolerance, because either would hide unrelated regressions.
